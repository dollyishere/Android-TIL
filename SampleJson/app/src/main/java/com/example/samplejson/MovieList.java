package com.example.samplejson;

/* 응답 데이터 가장 바깥쪽 {} 이므로 이 객체들을 반환할 클래스 */
public class MovieList {
    // boxOfficeResult 변수명은 반드시 JSON 문자열의 속성명과 일치!!!
    MovieListResult boxOfficeResult;

    // boxOfficeResult의 값은 객체이므로 이 객체를 담을 MovieListResult를 선언함
}
